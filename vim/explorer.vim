" keep current directory and browsing directory in sync
let g:netrw_keepdir = 0

" set explorer window to 30% of screen width when split
let g:netrw_winsize = 30

" disable the banner at the top of the explorer window
let g:netrw_banner = 0

" disable directory history
let g:netrw_dirhistmax = 0